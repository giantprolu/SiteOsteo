---
const navItems = [
  { href: "#accueil", text: "Accueil" },
  { href: "#services", text: "Services" },
  { href: "#contact", text: "Contact" },
];
---

<header class="fixed w-full bg-white/95 shadow-sm z-50">
  <div class="container mx-auto px-4 py-4 flex justify-between items-center">
    <a href="#accueil" class="text-primary-600 font-serif text-xl font-semibold">
      Quentin Philipot
      <span class="block text-sm text-gray-600">Ostéopathe D.O. à Aigondigné</span>
    </a>
    
    <nav class="hidden md:flex items-center gap-8">
      {navItems.map(item => (
        <a href={item.href} class="text-gray-600 hover:text-primary-600 transition-colors">
          {item.text}
        </a>
      ))}
      <a
        href="https://www.doctolib.fr/osteopathe/aigondigne/quentin-philipot"
        target="_blank"
        rel="noopener noreferrer"
        class="bg-primary-600 text-white px-4 py-2 rounded-md hover:bg-primary-700 transition-colors"
      >
        Prendre rendez-vous
      </a>
    </nav>

    <button class="md:hidden text-gray-600" aria-label="Menu">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </div>
</header>